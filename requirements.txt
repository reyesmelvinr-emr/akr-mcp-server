# AKR MCP Server Dependencies
# Python 3.10+ required

# MCP SDK - Model Context Protocol for VS Code Copilot integration
# Version pinned to prevent breaking changes from decorator/return-type churn
# Includes [cli] extras for mcp command-line tool
mcp[cli]>=1.26.0,<1.27

# Template rendering (Jinja2 migration - Phase 3)
Jinja2>=3.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# Logging and utilities
python-json-logger>=2.0.0

# YAML
PyYAML>=6.0.1
# JSON Schema validation (Phase 3 - front-matter validation)
jsonschema>=4.0.0